import { FlexGrowDirective } from './flex-grow.directive';

describe('FlexGrowDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexGrowDirective();
    expect(directive).toBeTruthy();
  });
});
