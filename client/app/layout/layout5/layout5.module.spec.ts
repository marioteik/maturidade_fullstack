import { Layout5Module } from './layout5.module';

describe('Layout4Module', () => {
  let layout4Module: Layout5Module;

  beforeEach(() => {
    layout4Module = new Layout5Module();
  });

  it('should create an instance', () => {
    expect(layout4Module).toBeTruthy();
  });
});
