import { Layout4Module } from './layout4.module';

describe('Layout4Module', () => {
  let layout4Module: Layout4Module;

  beforeEach(() => {
    layout4Module = new Layout4Module();
  });

  it('should create an instance', () => {
    expect(layout4Module).toBeTruthy();
  });
});
